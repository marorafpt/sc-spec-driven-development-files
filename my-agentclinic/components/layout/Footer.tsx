export default function Footer() {
  return (
    <footer className="site-footer">
      <p>© {new Date().getFullYear()} AgentClinic — Because even a language model deserves care.</p>
    </footer>
  );
}
